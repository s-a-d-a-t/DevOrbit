PORT=5000
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/devpulse
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES=7d
# Set MEMORY_DB=1 to run against an embedded dev PostgreSQL (auto-downloaded, data wiped each run)
MEMORY_DB=0
